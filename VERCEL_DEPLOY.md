# 🚀 Vercel Deployment Instructions

## Структура проекта

У нас есть 3 варианта прототипа в отдельных Git ветках:

- **`prototype-v1`** - Минимальная версия (Google → Template → Editor, без Signup)
- **`prototype-v2`** - Стандартная версия (Google → Template → Signup → Editor)
- **`prototype-v3`** - Полная версия (все функции)

Каждая ветка будет задеплоена на отдельный URL.

---

## 📋 Шаг 1: Push веток на GitHub

```bash
cd "/Users/anna/Documents/cursor test/figma MCP test"

# Push всех веток
git push origin main
git push origin prototype-v1
git push origin prototype-v2
git push origin prototype-v3
```

---

## 📋 Шаг 2: Создать проект на Vercel

1. Зайди на [vercel.com](https://vercel.com)
2. Нажми **"Add New" → "Project"**
3. Выбери свой GitHub репозиторий
4. Нажми **"Import"**

---

## 📋 Шаг 3: Настроить основной деплой

### Configure Project:

- **Framework Preset**: Vite
- **Root Directory**: `figma MCP test`
- **Build Command**: `npm run build`
- **Output Directory**: `dist`

Нажми **"Deploy"** - это задеплоит ветку `main`

---

## 📋 Шаг 4: Включить автодеплой для всех веток

1. После первого деплоя, зайди в **Settings** проекта
2. Перейди в **Git** секцию
3. Найди **"Production Branch"** - оставь `main`
4. В секции **"Automatic deployments from branches"** включи все ветки

---

## 🎯 Результат

После настройки, каждая ветка будет автоматически деплоиться:

### URLs будут примерно такими:

```
Main:
https://your-project.vercel.app

V1 (Minimal):
https://your-project-git-prototype-v1-your-username.vercel.app

V2 (Standard):
https://your-project-git-prototype-v2-your-username.vercel.app

V3 (Full):
https://your-project-git-prototype-v3-your-username.vercel.app
```

---

## 🔗 Шаг 5: Получить точные URLs

После деплоя:

1. Зайди в Vercel Dashboard
2. Открой свой проект
3. Нажми на **"Deployments"** вверху
4. Увидишь список всех деплоев по веткам
5. Каждый деплой имеет свой URL - кликни и скопируй

---

## 📤 Для User Testing

Дай участникам тестирования конкретные URLs:

**Группа A** → URL ветки `prototype-v1` (минимальная версия)
**Группа B** → URL ветки `prototype-v2` (стандартная версия)
**Группа C** → URL ветки `prototype-v3` (полная версия)

Участники не смогут переключаться между вариантами! 🔒

---

## 🔄 Обновление прототипов

Чтобы обновить любой вариант:

```bash
# Переключись на нужную ветку
git checkout prototype-v1

# Внеси изменения
# ...

# Закоммить и запушить
git add .
git commit -m "Update prototype V1"
git push origin prototype-v1
```

Vercel автоматически задеплоит обновление! ✨

---

## ⚙️ Дополнительные настройки (опционально)

### Установить кастомные домены для каждой ветки:

1. В Vercel Dashboard → Domains
2. Добавь домены:
   - `v1.yourdomain.com` → ветка `prototype-v1`
   - `v2.yourdomain.com` → ветка `prototype-v2`
   - `v3.yourdomain.com` → ветка `prototype-v3`

---

## 🎉 Готово!

Теперь у тебя есть 3 изолированных прототипа, каждый на своем URL! 🚀
